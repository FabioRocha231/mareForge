[package]
name = "mareforge-domain-economy"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
mareforge-shared = { path = "../shared" }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
chrono = { version = "0.4", features = ["serde"] }
